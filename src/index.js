import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Import do Css global (Reset e Config. Base)
import './css/global.css';
// Render do App no DOM
ReactDOM.render(<App />, document.getElementById('root'));
